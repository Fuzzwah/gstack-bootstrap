interface:
  display_name: "gstack-office-hours"
  short_description: "Product Office Hours — two modes. Product mode: six forcing questions that expose demand reality, status quo,..."
  default_prompt: "Use gstack-office-hours for this task."
policy:
  allow_implicit_invocation: true
